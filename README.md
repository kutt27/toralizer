# Toralizer

Install tor to the system

```sh
sudo apt install tor && sudo systemctl start tor
```

Compilation (gcc/clang):

```sh
gcc toralizer.c -o Toralizer
# clang toralizer.c -o toralizer
```


